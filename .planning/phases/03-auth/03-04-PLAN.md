---
phase: 03-auth
plan: "04"
type: execute
wave: 4
depends_on:
  - "03-03"
files_modified:
  - frontend/login.html
  - requirements.txt
  - render.yaml
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02

user_setup:
  - service: render
    why: "New JWT signing and admin credentials must be set as secret env vars in Render Dashboard"
    env_vars:
      - name: SECRET_KEY
        source: "Generate locally: openssl rand -hex 32 — then paste into Render Dashboard -> Environment"
      - name: ADMIN_SECRET
        source: "Choose a strong random string (e.g. openssl rand -hex 16) — set in Render Dashboard"
      - name: ADMIN_PASSWORD
        source: "Choose a strong password — set in Render Dashboard (default 'changeme' logs a warning)"
    dashboard_config:
      - task: "Set ADMIN_USERNAME if you want a non-default admin username (default: admin)"
        location: "Render Dashboard -> Service -> Environment"

must_haves:
  truths:
    - "Visiting /login returns an HTML page with a username + password form"
    - "Submitting valid credentials redirects the browser to / (or ?next= param)"
    - "Submitting invalid credentials shows 'Invalid credentials' inline — no redirect"
    - "After successful login, localStorage['bdda_token'] is set to the returned JWT"
    - "On every authenticated fetch, the frontend checks X-New-Token header and updates localStorage if present"
    - "An unauthenticated page redirects to /login?next=<original_url>"
    - "PyJWT and pwdlib[bcrypt] are listed in requirements.txt"
    - "render.yaml lists SECRET_KEY, ADMIN_SECRET, ADMIN_USERNAME, ADMIN_PASSWORD env vars"
  artifacts:
    - path: "frontend/login.html"
      provides: "Functional (unstyled) login page — Phase 5 redesigns with Tailwind"
      contains: "/api/auth/token"
    - path: "requirements.txt"
      provides: "PyJWT==2.11.0 and pwdlib[bcrypt]==0.3.0 added"
      contains: "PyJWT"
    - path: "render.yaml"
      provides: "4 new env vars for auth (SECRET_KEY sync:false, ADMIN_SECRET sync:false, ADMIN_USERNAME value:admin, ADMIN_PASSWORD sync:false)"
      contains: "SECRET_KEY"
  key_links:
    - from: "frontend/login.html:fetch('/api/auth/token')"
      to: "POST /api/auth/token"
      via: "URLSearchParams(formData) body — x-www-form-urlencoded content type"
      pattern: "URLSearchParams"
    - from: "frontend/login.html:localStorage"
      to: "bdda_token key"
      via: "localStorage.setItem('bdda_token', data.access_token) on success"
      pattern: "bdda_token"
---

<objective>
Create the login HTML page, update requirements.txt with auth packages, and update render.yaml with new env vars.

Purpose: The final deliverable — a working /login page that the browser can use, confirmed auth dependencies in requirements.txt, and documented Render env vars so the next deploy works without manual digging.

Output: frontend/login.html (functional login page), updated requirements.txt, updated render.yaml.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-auth/03-CONTEXT.md
@.planning/phases/03-auth/03-RESEARCH.md
@requirements.txt
@render.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create frontend/login.html — functional login page</name>
  <files>frontend/login.html</files>
  <action>
Create frontend/login.html as a self-contained HTML page. This is a FUNCTIONAL but UNSTYLED page per CONTEXT.md Area D — Phase 5 will redesign it with Tailwind + Alpine.js. Do not add any styling libraries now.

Requirements from CONTEXT.md Area D:
- Username + password form
- Submit → POST /api/auth/token as application/x-www-form-urlencoded (use URLSearchParams — NOT JSON)
- On success: store JWT in localStorage['bdda_token'], redirect to ?next= param if present, else /
- On failure: show "Invalid credentials" inline (no page reload)
- Page itself is public — no auth check needed to view the login page

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BDDA — Login</title>
    <style>
        body { font-family: sans-serif; max-width: 400px; margin: 100px auto; padding: 0 20px; }
        h1 { font-size: 1.4rem; margin-bottom: 1.5rem; }
        label { display: block; margin-bottom: 0.25rem; font-size: 0.9rem; }
        input { display: block; width: 100%; padding: 0.5rem; margin-bottom: 1rem;
                box-sizing: border-box; font-size: 1rem; border: 1px solid #ccc; border-radius: 4px; }
        button { padding: 0.6rem 1.5rem; font-size: 1rem; cursor: pointer; }
        #error-msg { color: red; margin-top: 1rem; display: none; }
    </style>
</head>
<body>
    <h1>BDDA Inspector Login</h1>
    <form id="login-form">
        <label for="username">Username</label>
        <input type="text" id="username" name="username" autocomplete="username" required autofocus>

        <label for="password">Password</label>
        <input type="password" id="password" name="password" autocomplete="current-password" required>

        <button type="submit">Log in</button>
        <p id="error-msg">Invalid credentials. Please try again.</p>
    </form>

    <script>
        // If already logged in, redirect away immediately
        (function() {
            const token = localStorage.getItem('bdda_token');
            if (token) {
                const params = new URLSearchParams(window.location.search);
                window.location.href = params.get('next') || '/';
            }
        })();

        document.getElementById('login-form').addEventListener('submit', async function(e) {
            e.preventDefault();
            const errorEl = document.getElementById('error-msg');
            errorEl.style.display = 'none';

            const formData = new FormData(this);

            let resp;
            try {
                // CRITICAL: use URLSearchParams — OAuth2PasswordRequestForm requires
                // application/x-www-form-urlencoded, NOT application/json
                resp = await fetch('/api/auth/token', {
                    method: 'POST',
                    body: new URLSearchParams(formData),
                    headers: { 'Content-Type': 'application/x-www-form-urlencoded' }
                });
            } catch (networkErr) {
                errorEl.textContent = 'Network error — is the server running?';
                errorEl.style.display = 'block';
                return;
            }

            if (!resp.ok) {
                errorEl.textContent = 'Invalid credentials. Please try again.';
                errorEl.style.display = 'block';
                return;
            }

            const data = await resp.json();
            localStorage.setItem('bdda_token', data.access_token);

            // Redirect to ?next= param or default to /
            const params = new URLSearchParams(window.location.search);
            window.location.href = params.get('next') || '/';
        });
    </script>
</body>
</html>
```

Note: The `/login` route is served by the GET /login endpoint added in Plan 03 (api.py). The static file mount also serves `frontend/login.html` as a fallback — having it in frontend/ ensures it works either way. The explicit GET /login route registered before the static mount takes precedence and is what browsers hit.
  </action>
  <verify>
Check file exists and has critical content:
```bash
grep -c "URLSearchParams" /Users/fabien/Desktop/CLAUDE/applications/drone/bdda/.claude/worktrees/frosty-banach/frontend/login.html
grep -c "bdda_token" /Users/fabien/Desktop/CLAUDE/applications/drone/bdda/.claude/worktrees/frosty-banach/frontend/login.html
grep -c "/api/auth/token" /Users/fabien/Desktop/CLAUDE/applications/drone/bdda/.claude/worktrees/frosty-banach/frontend/login.html
```
Each grep should return 1 or more (at least one match each). Zero = something missing.
  </verify>
  <done>
- frontend/login.html exists with username + password form
- Form submits to /api/auth/token using URLSearchParams (x-www-form-urlencoded)
- On success: stores JWT in localStorage['bdda_token'], redirects to ?next= or /
- On failure: shows "Invalid credentials" inline without page reload
- If already logged in: immediately redirects (no double-login loop)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update requirements.txt and render.yaml for auth packages and env vars</name>
  <files>requirements.txt, render.yaml</files>
  <action>
--- CHANGE 1: Update requirements.txt ---

Add the two auth libraries under the `# ─── Auth ─────` section (create this section if it doesn't exist). Place it after the existing `# ─── Database ─────────────────────────────────────────────────────────────────` section:

```
# ─── Auth ─────────────────────────────────────────────────────────────────────
PyJWT==2.11.0          # JWT encode/decode (replaces abandoned python-jose)
pwdlib[bcrypt]==0.3.0  # Password hashing (replaces abandoned passlib)
```

Do NOT add python-jose or passlib — these are abandoned libraries.
Do NOT add pwdlib[argon2] — CONTEXT.md locks bcrypt as the algorithm.

--- CHANGE 2: Update render.yaml ---

Read render.yaml first. Under the `envVars:` block for the web service, ADD these 4 env vars (do not remove existing ones):

```yaml
      - key: SECRET_KEY
        sync: false          # Set in Render Dashboard — generate with: openssl rand -hex 32
      - key: ADMIN_SECRET
        sync: false          # Set in Render Dashboard — protects /api/admin/create-user
      - key: ADMIN_USERNAME
        value: admin         # Default admin username — safe to commit
      - key: ADMIN_PASSWORD
        sync: false          # Set in Render Dashboard — default 'changeme' logs a warning
```

`sync: false` means Render prompts for the value on first deploy via Dashboard; the value is never stored in the repo. This is the Render-native pattern for secrets (per RESEARCH.md Env Vars section).

Also: remove any mention of `python-jose` or `passlib` from render.yaml if they appear (STATE.md incorrectly listed them as stack additions — they are NOT used).

--- CHANGE 3: Add GET /login route to api.py ---

Wait — this was supposed to be in Plan 03. Check if GET /login is already in api.py. If not, add it now BEFORE the static mount line:

```python
@app.get("/login", response_class=HTMLResponse)
async def login_page():
    """Serve the login page. Route registered before static mount — takes precedence."""
    from fastapi.responses import HTMLResponse
    login_path = FRONTEND_DIR / "login.html"
    if login_path.exists():
        return HTMLResponse(content=login_path.read_text())
    return HTMLResponse(content="<h1>Login page not found</h1>", status_code=404)
```

Make sure `HTMLResponse` is imported from fastapi.responses at the top of api.py if not already there.
  </action>
  <verify>
```bash
# Check requirements.txt has auth packages
grep "PyJWT" /Users/fabien/Desktop/CLAUDE/applications/drone/bdda/.claude/worktrees/frosty-banach/requirements.txt
grep "pwdlib" /Users/fabien/Desktop/CLAUDE/applications/drone/bdda/.claude/worktrees/frosty-banach/requirements.txt

# Check render.yaml has new env vars
grep "SECRET_KEY" /Users/fabien/Desktop/CLAUDE/applications/drone/bdda/.claude/worktrees/frosty-banach/render.yaml
grep "ADMIN_SECRET" /Users/fabien/Desktop/CLAUDE/applications/drone/bdda/.claude/worktrees/frosty-banach/render.yaml

# Check no abandoned libraries listed
grep -c "python-jose" /Users/fabien/Desktop/CLAUDE/applications/drone/bdda/.claude/worktrees/frosty-banach/requirements.txt || echo "python-jose: absent (correct)"
grep -c "passlib" /Users/fabien/Desktop/CLAUDE/applications/drone/bdda/.claude/worktrees/frosty-banach/requirements.txt || echo "passlib: absent (correct)"
```
Expected: PyJWT and pwdlib lines appear; SECRET_KEY and ADMIN_SECRET in render.yaml; python-jose and passlib are absent.
  </verify>
  <done>
- requirements.txt has PyJWT==2.11.0 and pwdlib[bcrypt]==0.3.0 under Auth section
- requirements.txt does NOT have python-jose or passlib
- render.yaml has SECRET_KEY (sync:false), ADMIN_SECRET (sync:false), ADMIN_USERNAME (value:admin), ADMIN_PASSWORD (sync:false)
- api.py has GET /login route returning HTMLResponse from frontend/login.html
- HTMLResponse imported in api.py
  </done>
</task>

</tasks>

<verification>
1. `grep PyJWT requirements.txt` returns a match
2. `grep pwdlib requirements.txt` returns a match
3. `grep SECRET_KEY render.yaml` returns a match
4. `grep python-jose requirements.txt` returns nothing (or grep exit code 1)
5. frontend/login.html exists with URLSearchParams and bdda_token
6. Final smoke test — start the server locally and visit http://localhost:8000/login:
   ```bash
   cd /Users/fabien/Desktop/CLAUDE/applications/drone/bdda/.claude/worktrees/frosty-banach
   DATA_DIR=./data SECRET_KEY=$(python -c "import secrets; print(secrets.token_hex(32))") ADMIN_PASSWORD=testpass123 python backend/api.py &
   sleep 3
   curl -s http://localhost:8000/login | grep -q "BDDA" && echo "Login page served: OK" || echo "Login page: FAIL"
   curl -s -X POST http://localhost:8000/api/auth/token -d "username=admin&password=testpass123" | grep -q "access_token" && echo "Login endpoint: OK" || echo "Login endpoint: FAIL"
   kill %1
   ```
</verification>

<success_criteria>
Phase 3 complete when all 5 success criteria from ROADMAP.md are met:
1. POST /api/auth/token returns JWT for valid credentials
2. POST /api/upload returns 401 without valid token
3. GET /api/jobs returns only jobs belonging to authenticated inspector
4. POST /api/admin/create-user creates new inspector (protected by ADMIN_SECRET header)
5. Login page at /login redirects to dashboard on success
</success_criteria>

<output>
After completion, create `.planning/phases/03-auth/03-04-SUMMARY.md` following the summary template.
</output>
